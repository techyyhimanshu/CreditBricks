
export const isAuthorizedRoute = (allowedRoute: string[], path: string): boolean => {
  // return allowedRoute.some(route => {
  //   return path.includes(route)
  // });
  console.log(allowedRoute)
  console.log(path)

  return true
};

